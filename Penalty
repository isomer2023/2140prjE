 # 惩罚 Battery 0
            if storage_charge[0] < 0.625 and storage_charge[0]*0.625 < action[0] <= 1:
                reward += -1.0
                print("  Battery 0 is empty and agent tries to discharge. Penalty applied.")

            if storage_charge[0] > 14.375 and -1 <= action[0] < (14.375-storage_charge[0])/0.625:  
                reward += -1.0
                print("  Battery 0 is full and agent tries to charge. Penalty applied.")

            # 惩罚 Battery 1
            if storage_charge[1] < 0.2916 and storage_charge[0]/0.2916 < action[0] <= 1:
                reward += -1.0
                print("  Battery 1 is empty and agent tries to discharge. Penalty applied.")

            if storage_charge[1] > 6.7084 and -1 <= action[0] < (6.7084-storage_charge[0])/0.2916:  
                reward += -1.0
                print("  Battery 1 is full and agent tries to charge. Penalty applied.")
