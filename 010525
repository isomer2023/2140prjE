            # Penalty Battery 0
            if storage_charge[0] < 0.625 and 0 < action[0] <= 1:
                reward += -1
            if storage_charge[0] > 14.375 and -1 <= action[0] < 0:
                reward += -1
            # Penalty Battery 1
            if storage_charge[1] < 0.292 and 0 < action[0] <= 1:
                reward += -1
            if storage_charge[1] > 6.708 and -1 <= action[0] < 0:
                reward += -1

            if np.any(env.tracker.state.rho >= 1.1):
                if -1 <= action[0] <= 1 and -1 <= action[1] <= 1:
                    reward += 3
                else:
                    reward += -100
            if np.any(env.tracker.state.rho >= 1.0):
                if -1 <= action[0] <= 1 and -1 <= action[1] <= 1:
                    reward += 2
                else:
                    reward += -10
            if np.any(env.tracker.state.rho >= 0.9):
                if -1 <= action[0] <= 1 and -1 <= action[1] <= 1:
                    reward += 0.1
                else:
                    reward += -0.1
            if np.any(env.tracker.state.rho >= 0.8):
                if -1 <= action[0] <= 1 or -1 <= action[1] <= 1:
                    reward += 0.1
                else:
                    reward += -0.1

            if stage.upper() == "TRAIN":
